import { ObjectProps } from "../../types/object-props"
import * as THREE from 'three';
import React from 'react';

export type MeshStandardMaterialProps = ObjectProps<{}, typeof THREE.MeshStandardMaterial, THREE.MeshStandardMaterial>

export const MeshStandardMaterial = (props: MeshStandardMaterialProps) => {
    return <></>
}