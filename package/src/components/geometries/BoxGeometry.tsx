import { ObjectProps } from "../../types/object-props";
import * as THREE from 'three';
import React from 'react';

export type BoxGeometryProps = ObjectProps<{}, typeof THREE.BoxGeometry, THREE.BoxGeometry>;

export const BoxGeometry = (props: BoxGeometryProps) => {
    return <></>
}