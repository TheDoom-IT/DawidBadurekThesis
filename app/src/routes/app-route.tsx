import React from "react"
import { App } from "../components/routes/app"

export const AppRoute = () => {
    return (
        <React.StrictMode>
            <App />
        </React.StrictMode>
    )
}